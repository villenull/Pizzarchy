Fixture tree for the T12 upstream-patch seam.

These are VERBATIM COPIES of upstream files, not adaptations. They are byte
identical to what the pinned runtime installs under /usr/share/omarchy, which is
the only reason the tests are worth anything: a hand-written approximation of the
context lines would let a patch pass here and reject on the device.

  Source      https://github.com/omacom/omarchy (moved from basecamp/omarchy;
                both remotes resolve tag v4.0.4 to the identical SHA)
  Ref         c668141e         (the RUNTIME pin = tag v4.0.4, Omarchy 4.0.4
                                stable; see iso/RUNTIME)
  Fetched     2026-09-17, via `git show c668141e:<path>` from a full clone
  Licence     MIT (upstream repo)

  shell/plugins/lock/Service.qml     551 lines
    sha256 3f0a265a09f7957e8f5d74666595d3136ec64899c414426190460f9e2098e701
  default/limine/limine.conf          20 lines
    sha256 282f055fdd439301c2249fc3ca579d88621080c8c4df150d6a15555b7c8581df
  default/foot/screensaver.ini         7 lines
    sha256 053150146bccb101eb2f14e0622376cff4d62563c3b1a16ae7dd8668be7ac51c

Replaces test/fixtures/t12-omarchy-f0020448ca87 (the 4.0.0 pin), retired
2026-09-17 with the 4.0.4 rebase. All three files are byte-identical between
f0020448 and c668141e (verified by cmp against `git show` at both refs), so
this is a rename, not a content change: all three shipped patches still
apply, and the delta classification lives in
docs/findings/v404-delta-classification.md.

⚠️ WHEN iso/RUNTIME MOVES, THIS DIRECTORY MUST MOVE WITH IT. Refetch both
files at the new ref, rename the directory, and re-run
test/unit/test-t12-patch-applier.sh. If a shipped patch no longer applies to
the refreshed fixture that is not a test bug -- it is the seam doing its one
job, and the patch needs a rebase. The suite asserts the directory name matches
iso/RUNTIME so a stale fixture cannot sit here unnoticed.

The drift fixture for `interval: 3000` (upstream commit 35a6940, "Just 3
seconds of screen on after lock") is not stored: the suite derives it from
Service.qml by substituting the literal, which reproduces upstream's content in
that neighbourhood exactly and cannot go stale independently of the file above.
